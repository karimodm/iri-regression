defaults: &db_2
  db: #Path to db.tar 
  db_checksum: #path to db.tar checksum


seeds: # For internal use by the regression system. 
    - SEED
    - SIID

nodes: 
  nodeA: #name
    <<: *db_2
  
  nodeB:
    <<: *db_2
